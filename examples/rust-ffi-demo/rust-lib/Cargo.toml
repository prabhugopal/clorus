[package]
name = "example-rust-lib"
version = "0.1.0"
edition = "2021"
description = "Example Rust library for testing automatic FFI generation"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
