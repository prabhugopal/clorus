[package]
name = "clorus-types"
version = "0.1.0"
edition = "2021"
description = "Canonical type system for Clorus FFI - single source of truth"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
