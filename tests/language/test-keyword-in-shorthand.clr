(ns test-keyword-in-shorthand)

;; The CORE pattern that was failing: keyword call in shorthand fn
(def m {:x 42})
(def f #((:x %)))

(defn -main []
  (println "Keyword call in shorthand function works!"))
