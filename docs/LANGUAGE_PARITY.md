# Language Parity

Canonical language parity status now lives in:

- `docs/generated/PARITY_STATUS.md`
- `docs/generated/STATUS.json`

Use `docs/reference/LANGUAGE_SPEC.md` for the language reference/spec surface.

## Update Command

```bash
python3 scripts/docs/build_status.py
```
